---
phase: 02-feedback-e-migracao-nextjs
plan: 02
type: execute
wave: 2
depends_on: ["02-01-PLAN.md"]
files_modified:
  - "lib/schemas/feedback.ts"
  - "lib/ratelimit.ts"
  - "app/api/feedback/route.ts"
  - ".env.local (documentação de variáveis)"
autonomous: true
requirements: [L2-L3-AUTONOMY, L2-L3-DESIGN]
user_setup:
  - "Criar conta Upstash em https://upstash.com — criar banco Redis, copiar UPSTASH_REDIS_REST_URL e UPSTASH_REDIS_REST_TOKEN"
  - "Criar projeto Supabase em https://supabase.com — executar o schema SQL abaixo no SQL Editor"
  - "Criar arquivo .env.local com as variáveis listadas em 'Variáveis de Ambiente'"
  - "Gerar IP_HASH_SECRET: `openssl rand -hex 32`"
  - "Gerar ADMIN_SECRET: `openssl rand -hex 32`"

must_haves:
  truths:
    - "POST /api/feedback com payload válido retorna 201 { success: true }"
    - "POST /api/feedback com conteúdo < 10 chars retorna 400"
    - "POST /api/feedback após 5 envios do mesmo IP retorna 429"
    - "POST /api/feedback com honeypot preenchido retorna 201 silencioso (sem persistir)"
    - "IP nunca armazenado em texto puro na tabela — apenas ip_hash"
    - "Tabela feedback com RLS habilitado e zero políticas públicas"
  artifacts:
    - "lib/schemas/feedback.ts"
    - "lib/ratelimit.ts"
    - "app/api/feedback/route.ts"
  key_links:
    - "ratelimit.ts usa Redis.fromEnv() — requer UPSTASH_REDIS_REST_URL e UPSTASH_REDIS_REST_TOKEN no ambiente"
    - "route.ts usa createClient() do Supabase com service_role key — nunca expor no client"
    - "IP_HASH_SECRET deve estar no .env.local e nas env vars da Vercel"
---

<objective>
**Objective:** Implementar a camada de backend da feature de feedback: schema SQL, validação Zod, rate limiting e route handler seguro.

**Purpose:** Demonstrar design técnico com decisões de segurança documentadas e implementadas (L2-L3-DESIGN) — hash de IP, RLS, rate limiting, honeypot.

**Output:**
- `lib/schemas/feedback.ts` — Zod schema com validação completa
- `lib/ratelimit.ts` — configuração Upstash sliding window
- `app/api/feedback/route.ts` — POST handler com todos os controles de segurança
- Schema SQL documentado para execução manual no Supabase
</objective>

<execution_context>
@.planning/phases/02-feedback-e-migracao-nextjs/02-CONTEXT.md
</execution_context>

<context>
@.planning/phases/02-feedback-e-migracao-nextjs/02-CONTEXT.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependências e criar schema Zod + ratelimit</name>
  <files>
    - "package.json"
    - "lib/schemas/feedback.ts"
    - "lib/ratelimit.ts"
  </files>
  <action>
1. **Instalar pacotes:**
   ```bash
   pnpm add zod @upstash/ratelimit @upstash/redis @supabase/supabase-js
   ```

2. **Criar `lib/schemas/feedback.ts`:**
   ```typescript
   import { z } from 'zod'

   export const feedbackSchema = z.object({
     content: z
       .string()
       .min(10, 'Feedback muito curto.')
       .max(2000, 'Limite de 2000 caracteres.')
       .trim(),
     isAnonymous: z.boolean().default(true),
     name: z.string().max(100).trim().nullish(),
     email: z.string().email('Email inválido.').nullish(),
     honeypot: z.literal('').optional(),
   }).refine(
     (d) => d.isAnonymous || d.name || d.email,
     { message: 'Informe nome ou email para feedback identificado.', path: ['name'] }
   )

   export type FeedbackInput = z.infer<typeof feedbackSchema>
   ```

3. **Criar `lib/ratelimit.ts`:**
   ```typescript
   import { Ratelimit } from '@upstash/ratelimit'
   import { Redis } from '@upstash/redis'

   export const ratelimit = new Ratelimit({
     redis: Redis.fromEnv(),
     limiter: Ratelimit.slidingWindow(5, '1 h'),
     analytics: true,
     prefix: 'pdi:feedback',
   })
   ```

4. **Documentar schema SQL** (comentário no topo de `lib/ratelimit.ts` ou arquivo separado `docs/feedback-schema.sql`):
   ```sql
   CREATE TABLE feedback (
     id          UUID        DEFAULT gen_random_uuid() PRIMARY KEY,
     content     TEXT        NOT NULL CHECK (char_length(content) BETWEEN 10 AND 2000),
     is_anonymous BOOLEAN    DEFAULT TRUE NOT NULL,
     name        TEXT,
     email       TEXT,
     ip_hash     TEXT,
     created_at  TIMESTAMPTZ DEFAULT now()
   );

   CREATE INDEX ON feedback (created_at DESC);

   ALTER TABLE feedback ENABLE ROW LEVEL SECURITY;
   -- zero políticas públicas = anon não pode ler nem escrever
   ```
  </action>
  <verify>
    <auto-check>
- `pnpm build` compila `lib/schemas/feedback.ts` e `lib/ratelimit.ts` sem erros TypeScript.
- Nenhum import circular.
    </auto-check>
  </verify>
  <done>
- Zod, Upstash e Supabase instalados.
- Schema Zod com honeypot, refine de identificado, e limites de conteúdo.
- Ratelimit configurado com sliding window 5/hora.
- Schema SQL documentado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implementar route handler POST /api/feedback</name>
  <files>
    - "app/api/feedback/route.ts"
    - "lib/supabase/server.ts"
  </files>
  <action>
1. **Criar `lib/supabase/server.ts`:**
   ```typescript
   import { createClient as createSupabaseClient } from '@supabase/supabase-js'

   export function createClient() {
     return createSupabaseClient(
       process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.SUPABASE_SERVICE_ROLE_KEY!
     )
   }
   ```

2. **Criar `app/api/feedback/route.ts`:**
   ```typescript
   import { NextRequest, NextResponse } from 'next/server'
   import { createHash } from 'crypto'
   import { ratelimit } from '@/lib/ratelimit'
   import { feedbackSchema } from '@/lib/schemas/feedback'
   import { createClient } from '@/lib/supabase/server'

   function getIP(req: NextRequest): string {
     return (
       req.headers.get('x-forwarded-for')?.split(',')[0]?.trim() ??
       req.headers.get('x-real-ip') ??
       '127.0.0.1'
     )
   }

   export async function POST(req: NextRequest) {
     try {
       const ip = getIP(req)
       const { success, limit, remaining, reset } = await ratelimit.limit(ip)

       if (!success) {
         return NextResponse.json(
           { error: 'Muitas tentativas. Tente novamente mais tarde.' },
           {
             status: 429,
             headers: {
               'X-RateLimit-Limit': String(limit),
               'X-RateLimit-Remaining': String(remaining),
               'X-RateLimit-Reset': new Date(reset).toUTCString(),
               'Retry-After': String(Math.ceil((reset - Date.now()) / 1000)),
             },
           }
         )
       }

       const body = await req.json().catch(() => null)
       if (!body) {
         return NextResponse.json({ error: 'Payload inválido.' }, { status: 400 })
       }

       const parsed = feedbackSchema.safeParse(body)
       if (!parsed.success) {
         return NextResponse.json(
           { error: 'Dados inválidos.', details: parsed.error.flatten() },
           { status: 400 }
         )
       }

       const { content, isAnonymous, name, email, honeypot } = parsed.data

       if (honeypot !== undefined && honeypot !== '') {
         return NextResponse.json({ success: true }, { status: 201 })
       }

       const ipHash = createHash('sha256')
         .update(ip + process.env.IP_HASH_SECRET!)
         .digest('hex')

       const supabase = createClient()
       const { error: dbError } = await supabase.from('feedback').insert({
         content,
         is_anonymous: isAnonymous,
         name: isAnonymous ? null : (name ?? null),
         email: isAnonymous ? null : (email ?? null),
         ip_hash: ipHash,
       })

       if (dbError) {
         console.error('[feedback] db error:', dbError.message)
         return NextResponse.json({ error: 'Erro ao salvar.' }, { status: 500 })
       }

       return NextResponse.json({ success: true }, { status: 201 })
     } catch (err) {
       console.error('[feedback] unexpected:', err)
       return NextResponse.json({ error: 'Erro interno.' }, { status: 500 })
     }
   }

   export function GET() {
     return NextResponse.json({ error: 'Método não permitido.' }, { status: 405 })
   }
   ```

3. **Documentar variáveis de ambiente necessárias** em `docs/env.md` ou `.env.example`:
   ```bash
   # Upstash Redis
   UPSTASH_REDIS_REST_URL=
   UPSTASH_REDIS_REST_TOKEN=

   # Supabase
   NEXT_PUBLIC_SUPABASE_URL=
   NEXT_PUBLIC_SUPABASE_ANON_KEY=
   SUPABASE_SERVICE_ROLE_KEY=

   # Segurança
   IP_HASH_SECRET=      # openssl rand -hex 32
   ADMIN_SECRET=        # openssl rand -hex 32 (usado na Wave 3)
   ADMIN_PASSWORD=      # senha escolhida pelo usuário (Wave 3)
   ```
  </action>
  <verify>
    <auto-check>
- `pnpm build` compila a route sem erros.
- TypeScript não reclama de tipos no `supabase.from('feedback').insert(...)`.
    </auto-check>
  </verify>
  <done>
- `lib/supabase/server.ts` criado com service_role key.
- Route handler com: rate limiting, parse seguro de JSON, validação Zod, honeypot, hash de IP, persistência no Supabase.
- Métodos não permitidos retornam 405.
- Variáveis de ambiente documentadas.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: API de feedback funcional end-to-end</name>
  <what-built>
Backend completo da feature de feedback: schema Zod, rate limiting Upstash e route handler POST /api/feedback com todos os controles de segurança.
  </what-built>
  <how-to-verify>
Requer: .env.local configurado com as variáveis Upstash + Supabase + IP_HASH_SECRET, e schema SQL executado no Supabase.

1. `pnpm dev` rodando.
2. Testar com curl ou REST client:

```bash
# Envio válido anônimo — deve retornar 201
curl -X POST http://localhost:3000/api/feedback \
  -H "Content-Type: application/json" \
  -d '{"content":"Este é um feedback de teste válido com mais de 10 chars","isAnonymous":true,"honeypot":""}'

# Conteúdo curto — deve retornar 400
curl -X POST http://localhost:3000/api/feedback \
  -H "Content-Type: application/json" \
  -d '{"content":"curto","isAnonymous":true,"honeypot":""}'

# Honeypot preenchido — deve retornar 201 (silencioso, sem persistir)
curl -X POST http://localhost:3000/api/feedback \
  -H "Content-Type: application/json" \
  -d '{"content":"Bot tentando enviar feedback aqui","isAnonymous":true,"honeypot":"bot-value"}'
```

3. Verificar no Supabase Studio (Table Editor > feedback) que:
   - O primeiro envio aparece na tabela.
   - O envio com honeypot NÃO aparece na tabela.
   - O campo `ip_hash` contém um hash hex, não o IP puro.
  </how-to-verify>
  <resume-signal>Responda "ok" para prosseguir para Wave 3 (formulário UI + painel admin), ou descreva erros de API/banco.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Internet → /api/feedback | Input não confiável. Mitigação: Zod safeParse, rate limiting, honeypot. |
| App Server → Supabase | Apenas service_role key no servidor. Nunca exposta no client. |
| IP → ip_hash | SHA-256 com salt (IP_HASH_SECRET). Não reversível sem o salt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03 | Spoofing | x-forwarded-for | Medium | Mitigate | Usar apenas o primeiro valor do header; comportamento padrão da Vercel. |
| T-02-04 | Denial of Service | POST /api/feedback | High | Mitigate | Rate limiting 5/IP/hora via Upstash sliding window. |
| T-02-05 | Information Disclosure | ip_hash | Low | Mitigate | SHA-256 + salt; IP puro nunca armazenado. |
| T-02-06 | Tampering | Supabase RLS | High | Mitigate | Zero políticas públicas; anon role bloqueado. |
| T-02-07 | Spoofing | Honeypot bypass | Low | Accept | Bots sofisticados podem detectar honeypot; rate limiting é a defesa primária. |
</threat_model>

<verification>
- `pnpm build` sem erros TypeScript.
- POST /api/feedback retorna 201 para payload válido.
- POST /api/feedback retorna 400 para conteúdo < 10 chars.
- POST /api/feedback retorna 429 após 5 tentativas do mesmo IP.
- Registro aparece no Supabase com ip_hash (não IP puro).
- RLS ativo na tabela feedback.
</verification>

<success_criteria>
- [ ] Zod, Upstash e Supabase instalados sem conflitos de dependência.
- [ ] Schema SQL executado no Supabase com RLS habilitado.
- [ ] POST /api/feedback funcional com rate limiting, validação e hash de IP.
- [ ] Honeypot retorna 201 silencioso sem persistir.
- [ ] service_role key nunca exposta no client.
</success_criteria>

<output>
Criar `.planning/phases/02-feedback-e-migracao-nextjs/02-02-SUMMARY.md` quando concluído.
</output>
