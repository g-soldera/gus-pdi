---
phase: 05-unified-deployment-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/feature-flags.ts
  - lib/hooks/use-pdi-data.ts
  - lib/validators/integrity.ts
  - __tests__/integrity.test.ts
autonomous: true
requirements:
  - FF-01
  - FF-02
  - FF-03
  - FF-04
  - FF-05
  - FF-06
  - FF-07
  - FF-08
  - FF-09
  - DI-02
  - DI-05

must_haves:
  truths:
    - "Feature flags control whether frontend uses mock data or Supabase API per entity"
    - "usePDIData hook automatically falls back to mock data if API call fails"
    - "Integrity checks compare mock data vs DB data and report discrepancies"
    - "Database export script generates fallback mock file from current DB state"
  artifacts:
    - path: "lib/feature-flags.ts"
      provides: "Feature flag environment variable definitions and toggles"
      min_lines: 20
    - path: "lib/hooks/use-pdi-data.ts"
      provides: "Data fetching hook with feature flag routing and auto-fallback"
      min_lines: 50
    - path: "lib/validators/integrity.ts"
      provides: "Mock vs DB integrity comparator"
      min_lines: 40
    - path: "__tests__/integrity.test.ts"
      provides: "Automated integrity verification tests"
      min_lines: 30
  key_links:
    - from: "lib/hooks/use-pdi-data.ts"
      to: "lib/feature-flags.ts"
      via: "flag check before API fetch"
      pattern: "isFeatureEnabled"
    - from: "lib/hooks/use-pdi-data.ts"
      to: "lib/supabase/queries.ts"
      via: "fallback on error catch"
      pattern: "catch.*fallback"
---

<objective>
Implement feature flags, gradual migration hooks (`usePDIData`), auto-fallback logic, mock-vs-DB integrity tests, and database export rollback utilities.

Purpose: Safely bridge static mock data and the new Supabase backend with zero downtime and automatic error recovery.
Output: Feature flag helpers, hybrid data hook, integrity test suite, and export script.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/supabase/queries.ts
@lib/supabase/seeder.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: create feature flags system and usePDIData hook</name>
  <files>lib/feature-flags.ts, lib/hooks/use-pdi-data.ts</files>
  <behavior>
    - Test 1: Returns mock data when feature flag is false
    - Test 2: Fetches from API when feature flag is true
    - Test 3: Falls back to mock data gracefully when API request fails
  </behavior>
  <action>
    Create `lib/feature-flags.ts` exposing `isFeatureEnabled(entity)` checking `NEXT_PUBLIC_FEATURE_DB_SKILLS`, `NEXT_PUBLIC_FEATURE_DB_MILESTONES`, `NEXT_PUBLIC_FEATURE_DB_PROJECTS`, `NEXT_PUBLIC_FEATURE_DB_RESOURCES`, `NEXT_PUBLIC_FEATURE_DB_PERSONAL_INFO`.
    Create `lib/hooks/use-pdi-data.ts` implementing hybrid fetching with automatic fallback to `pdiData.ts` on network or server error, logging warnings.
  </action>
  <verify>
    <automated>npx vitest run lib/hooks/use-pdi-data.test.ts</automated>
  </verify>
  <done>Feature flags configured and hybrid hook functional with fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: create mock vs db integrity checker and export script</name>
  <files>lib/validators/integrity.ts, __tests__/integrity.test.ts, scripts/export-db.ts</files>
  <behavior>
    - Test 1: Compares mock arrays with DB arrays and reports structural parity
    - Test 2: Detects missing records or field mismatches between mock and DB
  </behavior>
  <action>
    Create `lib/validators/integrity.ts` to compare mock entities against live Supabase records (FF-09, DI-05).
    Create `__tests__/integrity.test.ts` to run comparison assertions.
    Create `scripts/export-db.ts` to dump current Supabase tables back into a valid `pdiData.ts` format for manual rollback (DI-02).
  </action>
  <verify>
    <automated>npx vitest run __tests__/integrity.test.ts</automated>
  </verify>
  <done>Integrity validator, automated test, and database export script established.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API / Supabase | Feature flag evaluation and data fallback execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | feature-flags.ts | accept | Feature flags are public toggles; no secrets exposed. |
| T-05-02 | Tampering | use-pdi-data.ts | mitigate | Validate all API payloads against Zod schemas before rendering. |
</threat_model>

<verification>
Automated vitest suites pass for feature flags, hooks, and integrity validation.
</verification>

<success_criteria>
Feature flags correctly toggle data sources, hooks fall back on failure, integrity tests verify sync, export script outputs valid fallback data.
</success_criteria>

<output>
After completion, create `.planning/phases/05-unified-deployment-and-polish/05-01-SUMMARY.md`
</output>
