---
phase: 05-unified-deployment-and-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - components/milestone-objective-modal.tsx
  - app/api/milestones/[id]/objectives/[objId]/route.ts
  - components/timeline.tsx
autonomous: false
requirements:
  - MCF-01
  - MCF-02
  - MCF-03
  - MCF-04
  - MCF-05
  - MCF-06
  - MCF-07
  - MCF-08
  - MCF-09
  - MCF-10

must_haves:
  truths:
    - "Clicking an objective checkbox opens a completion modal with justification textarea and admin password input"
    - "Submitting the modal calls PATCH /api/milestones/[id]/objectives/[objId] with justification and password verification"
    - "Milestones support inline editing of title, description, and deadline with authentication"
    - "Users receive clear toast success or error feedback upon completion attempts"
  artifacts:
    - path: "components/milestone-objective-modal.tsx"
      provides: "Interactive modal for objective completion with justification and password verification"
      min_lines: 60
    - path: "app/api/milestones/[id]/objectives/[objId]/route.ts"
      provides: "API endpoint for objective status update with justification audit and password check"
      min_lines: 45
  key_links:
    - from: "components/milestone-objective-modal.tsx"
      to: "/api/milestones/[id]/objectives/[objId]"
      via: "fetch PATCH request on submit"
      pattern: "fetch.*objectives"
    - from: "app/api/milestones/[id]/objectives/[objId]/route.ts"
      to: "lib/supabase/queries.ts"
      via: "database update query"
      pattern: "supabase.*update"
---

<objective>
Implement interactive milestone completion flows, objective justification modals, inline milestone editing, and the corresponding backend API endpoint with password verification.

Purpose: Enable secure, justified milestone progress tracking directly from the UI.
Output: Objective modal component, objective update API route, and timeline integration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/supabase/queries.ts
@lib/schemas/crud.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: create objective update api endpoint with justification and password auth</name>
  <files>app/api/milestones/[id]/objectives/[objId]/route.ts</files>
  <action>
    Create PATCH endpoint accepting `{ completed: boolean, justification: string, password: string }`.
    Verify password against `ADMIN_PASSWORD`. Validate payload with Zod schema. Update milestone objective status and append justification audit trail in DB.
  </action>
  <verify>
    <automated>npx vitest run __tests__/api-milestones.test.ts</automated>
  </verify>
  <done>Objective update API route active with authentication and audit logging.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Milestone objective completion modal and interactive timeline integration.
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Open http://localhost:3000
    3. Click an incomplete objective checkbox on any milestone card.
    4. Verify modal opens prompting for justification and admin password.
    5. Test invalid password / empty justification error handling.
    6. Test successful completion and confirm checkbox updates with toast feedback.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Modal → API endpoint | Unauthenticated write request requiring inline password verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Elevation of Privilege | objectives/[objId]/route.ts | mitigate | Enforce inline password verification against ADMIN_PASSWORD on every request. |
| T-05-04 | Tampering | milestone-objective-modal.tsx | mitigate | Validate justification length and sanitize input on both client and server. |
</threat_model>

<verification>
API test suite passes; human verification confirms modal interaction, validation, and success feedback.
</verification>

<success_criteria>
Interactive milestone completion flow is fully operational with justification capture and password protection.
</success_criteria>

<output>
After completion, create `.planning/phases/05-unified-deployment-and-polish/05-02-SUMMARY.md`
</output>
