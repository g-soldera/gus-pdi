---
phase: 01-pleno-ano-1-2026-2027
plan: 02
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - "src/components/StudyPath.tsx"
  - "src/components/Resources.tsx"
  - "src/types/pdi.ts"
  - "src/data/pdiData.ts"
  - "src/data/secmlopsPath.ts"
  - "src/data/resources.ts"
autonomous: true
requirements: [PLENO-02]

must_haves:
  truths:
    - "Trilha de estudos SecMLOps organizada por categorias e progresso visível"
    - "Sistema de referências materiais vinculado aos milestones do PDI"
    - "Dashboard consolidado mostra progresso geral e métricas de tempo"
    - "Documentação técnica da arquitetura existe e é acessível"
  artifacts:
    - "src/components/StudyPath.tsx" (componente de trilha de estudos)
    - "src/components/Resources.tsx" (componente de referências)
    - "src/data/secmlopsPath.ts" (dados da trilha SecMLOps)
    - "src/data/resources.ts" (catálogo de materiais por milestone)
  key_links:
    - "Componente `StudyPath` importa dados de `secmlopsPath.ts`"
    - "Componente `Resources` mapeia materiais para IDs de milestone"
    - "Dashboard consolida métricas de `pdiData.ts` e `secmlopsPath.ts`"
---

<objective>
**Objective:** Completar o sistema de gestão de PDI com trilha de estudos SecMLOps estruturada, catálogo de referências materiais e dashboard de progresso consolidado.

**Purpose:** Fortalecer a demonstração de autonomia técnica (PLENO-02) através da criação de uma ferramenta completa de desenvolvimento de carreira que vai além da interface básica, incluindo gestão de conhecimento especializado em AI Security.

**Output:**
- Trilha de estudos SecMLOps organizada por tópicos (Model Supply Chain, Runtime Guardrails, Adversarial ML, NIST AI RMF)
- Sistema de referências materiais (livros, artigos, cursos, certificações) vinculado a milestones
- Dashboard de progresso com KPIs de tempo e conclusão
- Documentação técnica da arquitetura do sistema React/TypeScript
</objective>

<execution_context>
@D:/Projects/gus-pdi/.opencode/gsd-core/workflows/execute-plan.md
@D:/Projects/gus-pdi/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-pleno-ano-1-2026-2027/01-CONTEXT.md
@.planning/phases/01-pleno-ano-1-2026-2027/01-01-PLAN-SUMMARY.md
@PDI_UPDATE_SPECS.md
@src/data/pdiData.ts
@src/types/pdi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar trilha de estudos SecMLOps</name>
  <files>
    - "src/data/secmlopsPath.ts"
    - "src/components/StudyPath.tsx"
    - "src/types/pdi.ts"
  </files>
<action>Implementar a trilha estruturada de estudos SecMLOps conforme D-03.

1.  **Criar `src/data/secmlopsPath.ts`:**
    - Definir interface `StudyTopic` com campos: `id`, `category`, `name`, `description`, `progress` (0-100), `resources` (array de resource IDs).
    - Criar 4 categorias principais: "Model Supply Chain", "Runtime Guardrails", "Adversarial ML", "NIST AI RMF".
    - Para cada categoria, adicionar 3-5 tópicos específicos (ex: ".safetensors validation", "MITRE ATLAS mapping", "Prompt injection detection").
    - Vincular tópicos aos milestones relevantes do PDI (usar milestone IDs).

2.  **Estender `src/types/pdi.ts`:**
    - Adicionar interface `StudyTopic` exportada.
    - Opcionalmente, adicionar campo `studyTopics?: string[]` em `Milestone` para referência bidirecional.

3.  **Criar `src/components/StudyPath.tsx`:**
    - Componente React que renderiza a trilha de estudos.
    - Layout: lista de categorias com tópicos agrupados.
    - Mostrar progresso por tópico (barra de progresso ou %).
    - Permitir expandir/recolher categorias.
    - Usar cores/ícones para distinguir categorias.

Implementar uma visualização clara do roadmap de aprendizado em AI Security.
</action>
<verify>
  <human-check>
1. Recarregue a aplicação React.
2. Navegue até a nova seção "Trilha SecMLOps" (adicionar link/rota se necessário).
3. Verifique:
   - As 4 categorias principais estão visíveis.
   - Cada categoria tem 3-5 tópicos específicos.
   - O progresso é mostrado visualmente para cada tópico.
   - Os tópicos têm links para os milestones relacionados (IDs).
4. Interaja com a interface (expandir/recolher categorias).
  </human-check>
</verify>
<done>
- Arquivo `secmlopsPath.ts` criado com dados estruturados da trilha.
- Tipo `StudyTopic` adicionado aos tipos do projeto.
- Componente `StudyPath` renderiza a trilha com progresso visual.
- Interface é navegável e informativa sobre o roadmap de aprendizado.
</done>
</task>

<task type="auto">
  <name>Task 2: Implementar sistema de referências materiais</name>
  <files>
    - "src/data/resources.ts"
    - "src/components/Resources.tsx"
    - "src/types/pdi.ts"
  </files>
<action>Criar catálogo de materiais de aprendizado vinculado aos milestones conforme D-07.

1.  **Criar `src/data/resources.ts`:**
    - Definir interface `Resource` com campos: `id`, `title`, `type` ("book", "article", "course", "certification", "paper", "tool"), `author`, `url`, `milestoneIds` (array), `status` ("pending", "in-progress", "completed").
    - Popular com 10-15 recursos relevantes para AI Security:
      - Livros: "Practical Deep Learning Security" (vários)
      - Artigos: OWASP Top 10 for LLMs, MITRE ATLAS papers
      - Cursos: Coursera ML Security, Udemy Adversarial ML
      - Certificações: AWS Security Specialty, CompTIA Security+
      - Ferramentas: NeMo Guardrails, MLflow, .safetensors
    - Vincular cada recurso a 1-3 milestone IDs relevantes.

2.  **Estender `src/types/pdi.ts`:**
    - Adicionar interface `Resource` exportada.
    - Adicionar campo `resources?: string[]` em `Milestone` para referência.

3.  **Criar `src/components/Resources.tsx`:**
    - Componente que lista recursos filtrável por tipo e milestone.
    - Mostrar status (pendente, em progresso, concluído) visualmente.
    - Links clicáveis para URLs externas.
    - Opção de filtrar por milestone específico.

Implementar um catálogo navegável de materiais de aprendizado.
</action>
<verify>
  <human-check>
1. Navegue até a seção "Referências" ou "Recursos" na aplicação.
2. Verifique:
   - Lista de 10-15 recursos organizados por tipo.
   - Cada recurso mostra tipo, título, autor e status.
   - Filtros funcionam (por tipo, por milestone).
   - Links abrem em nova aba quando clicados.
3. Teste o filtro por milestone: selecione um milestone específico e veja apenas recursos relacionados.
  </human-check>
</verify>
<done>
- Arquivo `resources.ts` com catálogo de materiais de AI Security.
- Tipo `Resource` adicionado aos tipos do projeto.
- Componente `Resources` com filtragem e navegação funcional.
- Sistema de referências integrado com milestones do PDI.
</done>
</task>

<task type="auto">
  <name>Task 3: Criar dashboard de progresso consolidado</name>
  <files>
    - "src/components/Dashboard.tsx"
    - "src/components/Hero.tsx" (atualização)
  </files>
<action>Implementar dashboard com métricas consolidadas de progresso conforme D-08.

1.  **Criar `src/components/Dashboard.tsx`:**
    - Componente que agrega métricas de múltiplas fontes:
      - Progresso geral do PDI (% milestones concluídos)
      - Tempo até targets (dias até Pleno formal, dias até Sênior)
      - Progresso da trilha SecMLOps (% por categoria)
      - Status de recursos (pendentes vs concluídos)
    - Usar cards/stats com visualizações simples (progress bars, números, porcentagens).
    - Calcular dinamicamente métricas baseadas nos dados atuais.

2.  **Atualizar `src/components/Hero.tsx`:**
    - Adicionar seção de "Resumo" no topo com 2-3 KPIs principais.
    - Incluir contador de dias até o próximo target (Pleno formal em 07/06/2026).
    - Mostrar progresso geral (% milestones concluídos).

3.  **Criar lógica de cálculo:**
    - Funções utilitárias para calcular métricas a partir dos dados.
    - Tratamento de datas (dias restantes até targets).
    - Agregação de progresso (média ponderada).

Implementar uma visão "executiva" do progresso do PDI.
</action>
<verify>
  <human-check>
1. Recarregue a aplicação React.
2. Examine a nova seção de dashboard (página principal ou seção dedicada).
3. Verifique:
   - KPIs mostram % de progresso geral do PDI.
   - Contador de dias até Pleno formal (07/06/2026) e Sênior (2028-01-01).
   - Progresso da trilha SecMLOps por categoria (4 barras/porcentagens).
   - Estatísticas de recursos (ex: "5/15 concluídos").
4. Confirme que os números mudam dinamicamente se você modificar dados nos arquivos.
  </human-check>
</verify>
<done>
- Componente `Dashboard` com métricas consolidadas.
- Hero atualizado mostra KPIs principais na página inicial.
- Cálculos dinâmicos baseados em dados atuais do PDI.
- Visão executiva clara do progresso da carreira e aprendizado.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| External URLs -> User | Links para recursos externos abrem em nova aba; usuário visita sites de terceiros. |
| Data Calculations -> Display | Métricas são calculadas a partir de dados estáticos; erros de cálculo mostram informação incorreta. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-04 | Spoofing | External Resource Links | Low | Mitigate | Links abrem em nova aba com `target="_blank" rel="noopener noreferrer"`. |
| T-01-05 | Information Disclosure | Calculated Metrics | Low | Accept | Métricas são públicas e não sensíveis; erros de cálculo são visuais apenas. |
| T-01-06 | Denial of Service | Dashboard Calculations | Low | Accept | Cálculos são simples e executados no cliente; impacto de performance é mínimo. |
</threat_model>

<verification>
- Dashboard mostra métricas calculadas corretamente.
- Links para recursos externos funcionam e abrem em nova aba.
- Componente `StudyPath` renderiza todas as 4 categorias com tópicos.
- Filtros no componente `Resources` funcionam corretamente.
</verification>

<success_criteria>
- [ ] Trilha de estudos SecMLOps organizada e visualmente clara.
- [ ] Catálogo de 10-15 recursos vinculados a milestones.
- [ ] Dashboard mostra KPIs consolidados de progresso.
- [ ] Sistema completo demonstra autonomia técnica avançada (PLENO-02).
- [ ] Aplicação React compila e executa sem erros.
</success_criteria>

<output>
Criar `.planning/phases/01-pleno-ano-1-2026-2027/01-02-PLAN-SUMMARY.md` quando concluído
</output>