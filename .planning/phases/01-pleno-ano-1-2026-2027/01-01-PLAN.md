---
phase: 01-pleno-ano-1-2026-2027
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/types/pdi.ts"
  - "src/data/pdiData.ts"
  - "src/components/Hero.tsx"
  - "src/components/Milestones.tsx"
  - "package.json"
autonomous: false
requirements: [PLENO-02]
user_setup: []

must_haves:
  truths:
    - "Timeline de carreira visual mostra histórica completa e targets futuros"
    - "Milestones despriorizados aparecem com ícone de arquivo e estilo diferenciado"
    - "Novos milestones de arquitetura e liderança técnica foram adicionados"
    - "Interface React funciona e carrega todos os dados atualizados"
  artifacts:
    - "src/types/pdi.ts" (com `archived: boolean`)
    - "src/data/pdiData.ts" (com milestones atualizados)
    - "src/components/Hero.tsx" (com timeline visual)
    - "src/components/Milestones.tsx" (com tratamento de arquivados)
  key_links:
    - "O campo `archived` no `Milestone` interface influencia o componente `Milestones`"
    - "A timeline em `Hero.tsx` usa os targets `timelineTarget` e `seniorTargetDate`"
---

<objective>
**Objective:** Implementar a estruturação visual e de dados do PDI com foco na transição de carreira e milestones avançados de arquitetura e liderança técnica.

**Purpose:** Demonstrar autonomia técnica e capacidade de comunicação (PLENO-02) através da implementação de uma interface React robusta para gestão de desenvolvimento de carreira, seguindo as especificações do `PDI_UPDATE_SPECS.md`.

**Output:**
- Sistema de tipos atualizado com suporte a milestones arquivados
- Dados de milestones refletindo transição Pleno → Sênior/Tech Lead
- Timeline visual da carreira substituindo barra de progresso estática
- Componente de milestones com tratamento visual para itens despriorizados
- Interface funcional demonstrando gestão de conhecimento de carreira
</objective>

<execution_context>
@D:/Projects/gus-pdi/.opencode/gsd-core/workflows/execute-plan.md
@D:/Projects/gus-pdi/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-pleno-ano-1-2026-2027/01-CONTEXT.md
@PDI_UPDATE_SPECS.md
@src/data/pdiData.ts (primeiras 200 linhas)
@src/types/pdi.ts
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "PDI structure update" — dados e interface básica</name>
  <files>
    - "src/types/pdi.ts"
    - "src/data/pdiData.ts"
    - "src/components/Hero.tsx"
  </files>
  <action>Implementar a primeira camada de atualização do PDI: atualizar tipos para suportar `archived`, adicionar milestones de arquitetura, e criar uma versão básica da timeline no Hero. O objetivo é provar que os dados fluem dos tipos para o componente.

1.  **Atualizar `src/types/pdi.ts`:**
    - Adicionar campo opcional `archived?: boolean` à interface `Milestone`.
    - Documentar o propósito: "Milestones com progresso < 50% podem ser marcados como despriorizados"

2.  **Atualizar `src/data/pdiData.ts`:**
    - Aplicar a regra de despriorização: para cada milestone existente com `progress < 50`, adicionar `archived: true`.
    - Atualizar `currentRole` para "Engenheiro de Analytics JR" e `targetRole` para "Engenheiro de Analytics PL" conforme especificação.
    - Adicionar `seniorTargetDate: "2028-01-01"` ao `personalInfo`.
    - Adicionar 2-3 novos milestones baseados na seção 1 do `PDI_UPDATE_SPECS.md` (ex: `arch-system-design`, `tech-lead-projects`).

3.  **Criar timeline básica em `Hero.tsx`:**
    - Substituir a barra de progresso atual (linhas ~133-145) por uma timeline horizontal simples.
    - Usar `<div>` com `flex` para mostrar marcos: "Freelancer", "Estagiário", "Júnior", "Pleno (atuando como)", "Sênior".
    - Destacar visualmente "Júnior → Pleno" e "Pleno → Sênior" com cores/estilos diferentes.
    - Usar os valores de `timelineTarget` e `seniorTargetDate` do `personalInfo`.

Wire uma única linha de dados (os primeiros 2 milestones) através dos tipos, dados e componente.
</action>
<verify>
  <human-check>
1. Execute `npm run dev` (ou comando equivalente) para iniciar o servidor de desenvolvimento.
2. Abra o navegador na aplicação React (geralmente http://localhost:5173).
3. Verifique:
   - A barra de progresso antiga foi substituída por uma linha do tempo horizontal.
   - A linha do tempo mostra "Júnior", "Pleno (atuando como)" com data 07/06/2026 e "Sênior" com data 2028-01-01.
   - Os primeiros milestones listados incluem os novos (ex: "Domínio em System Design").
4. Abra as ferramentas de desenvolvedor, vá para "Componentes React" (se disponível) ou "Elements", e confirme que os novos dados estão sendo renderizados.
  </human-check>
</verify>
<done>
- Os tipos de dados foram estendidos com `archived`.
- As regras de despriorização foram aplicadas aos milestones existentes.
- Novos milestones foram adicionados.
- A barra de progresso foi substituída por uma timeline funcional que usa os targets reais do PDI.
- A aplicação React compila e renderiza sem erros.
</done>
</task>

<task type="auto">
  <name>Task 2: Completar o tratamento de milestones arquivados</name>
  <files>
    - "src/components/Milestones.tsx"
    - "src/components/MilestoneCard.tsx" (se existir, ou criar)
    - "src/data/pdiData.ts"
  </files>
<action>Implementar a visualização completa dos milestones arquivados conforme D-02 e D-05.

1.  **Importar ícones (se necessário):**
    - Adicionar `import { Archive } from 'lucide-react';` (ou ícone equivalente do projeto).

2.  **Atualizar `Milestones.tsx`:**
    - Na função `sortedMilestones`, ajustar a lógica de ordenação para colocar milestones com `archived: true` no final.
    - Opcionalmente, criar uma lógica de filtragem para usuários poderem ver/hide arquivados.

3.  **Criar ou atualizar `MilestoneCard.tsx`:**
    - Adicionar lógica condicional: se `milestone.archived === true`, adicionar um ícone `<Archive />` no canto superior direito do card.
    - Aplicar estilos diferenciados (ex: `opacity: 0.7`, `background-color: #f5f5f5`, `border-color: #e0e0e0`) aos cards arquivados.
    - Criar um badge/indicador visual "Arquivado" ou "Despriorizado".

4.  **Finalizar `pdiData.ts`:**
    - Adicionar todos os 7 milestones da seção 1 do `PDI_UPDATE_SPECS.md` com suas métricas e datas-alvo.
    - Garantir que milestones com progresso baixo (ex: `cert-aws-cloud` com 40%) tenham `archived: true`.

Implementar uma verificação visual clara: usuário deve conseguir identificar facilmente quais milestones estão ativos vs arquivados.
</action>
<verify>
  <human-check>
1. Recarregue a aplicação React.
2. Navegue até a seção de milestones (visualização de cards ou timeline).
3. Confirme:
   - Milestones com progresso baixo (< 50%) aparecem com opacidade reduzida e/ou cor cinza.
   - O ícone de arquivo (ou algum indicador visual) aparece no canto superior direito desses cards.
   - Os 7 novos milestones da especificação estão presentes e visíveis.
   - A ordem de exibição mostra os arquivados por último.
4. Verifique se há 2 grupos visuais distintos: milestones ativos (cores vivas) e despriorizados (tons mais suaves).
  </human-check>
</verify>
<done>
- Milestones arquivados têm estilo visual diferenciado (opacidade, cor de fundo).
- Ícone de arquivo aparece nos cards despriorizados.
- Todos os 7 milestones da especificação foram adicionados com métricas completas.
- Lógica de ordenação coloca arquivados no final da lista.
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Review da interface de gestão de PDI</name>
  <what-built>
Implementamos as mudanças estruturais no PDI: timeline visual da carreira, milestones arquivados com tratamento visual, e dados completos baseados na especificação. Este checkpoint é para validar se a interface atende às necessidades de gestão de carreira e demonstra autonomia técnica.
  </what-built>
  <how-to-verify>
1. Abra a aplicação React em http://localhost:5173 (ou equivalente).
2. Examine a nova timeline de carreira no topo:
   - Ela mostra claramente a jornada histórica (Freelancer → Estagiário → Júnior) e o futuro (Pleno atuando → Sênior).
   - As transições de cargo são visualmente distintas.
   - As datas alvo (07/06/2026 e 2028-01-01) estão presentes.

3. Explore a seção de milestones:
   - Os 7 novos milestones (System Design, Tech Lead, IA Generativa, MLOps, AWS SA Pro, Cross-team, Mentoria) estão listados com suas métricas.
   - Milestones antigos com progresso baixo (< 50%) aparecem despriorizados (estilo diferenciado).
   - O ícone de arquivo/sinal visual indica claramente itens arquivados.
   - A ordenação coloca arquivados no final.

4. Avalie se esta interface:
   - Permite acompanhar o progresso da transição Pleno → Sênior/Tech Lead.
   - Torna visíveis os KPIs e entregas esperadas para cada milestone.
   - Facilita a priorização (itens ativos vs despriorizados).
   - Demonstra autonomia técnica na implementação de uma ferramenta de gestão.

5. Navegue entre as diferentes visualizações (cards, timeline, etc.) para verificar consistência.
  </how-to-verify>
  <resume-signal>Type "approved" para prosseguir com Phase 1 completada, ou descreva quaisquer problemas de UX/dados que precisem ser corrigidos antes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User Input -> Data Validation | Os dados do PDI são estáticos no frontend; não há entrada do usuário que precise de validação neste momento. |
| Development -> Production | Alterações em tipos TypeScript podem quebrar a compilação se não forem compatíveis com uso existente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | Dados estáticos do PDI | Low | Accept | Os dados são carregados do arquivo TypeScript `pdiData.ts` que está no controle do desenvolvedor. Não há modificação em runtime. |
| T-01-02 | Denial of Service | Interface React | Low | Accept | A aplicação é estática e local; performance depende apenas do navegador do usuário. |
| T-01-03 | Elevation of Privilege | N/A | N/A | N/A | Não aplicável — não há autenticação ou autorização nesta fase. |
</threat_model>

<verification>
- A aplicação React compila sem erros TypeScript (`npm run build` ou `npm run type-check`).
- A timeline mostra datas corretas e transições visuais claras.
- Milestones arquivados são visualmente distintos dos ativos.
- Todos os 7 novos milestones da especificação estão presentes.
</verification>

<success_criteria>
- [ ] O PDI reflete a transição Pleno → Sênior/Tech Lead conforme especificado.
- [ ] Milestones despriorizados são claramente identificáveis visualmente.
- [ ] A timeline substitui com sucesso a barra de progresso estática.
- [ ] A aplicação React funciona sem erros e demonstra autonomia técnica (PLENO-02).
- [ ] Checkpoint humano aprova a interface como adequada para gestão de carreira.
</success_criteria>

<output>
Criar `.planning/phases/01-pleno-ano-1-2026-2027/01-01-PLAN-SUMMARY.md` quando concluído
</output>